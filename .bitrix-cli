# Файл конфигурации bitrix-cli
# Здесь определяются конфигурация по-умолчанию.
# Этот файл необходимо скопировать в домашний каталог и отредактировать.
# Все переменные имеют префикс "_BX_" и могут быть перезаписаны через переменные окружения (с префиксом "BX_").
# Для активации необходимо раскомментировать необходимые строки.
# Указанные здесь параметры имеют более высокий приоритет, чем значения по-умолчанию, но меньший, чем переменные окружения.

# Путь к PHP. Значение по-умолчанию: `which php`
# _BX_PHP=/usr/bin/php

# Язык сайта (Битрикс поддерживает многоязычность, устанавливая константу `LANGUAGE_ID`)
# _BX_LANGUAGE_ID=ru

# Уровень ошибок. Переопределяет установленный в Битриксе уровень ошибок в прологе `/prolog_before.php` через `error_reporting()`.
# Должно быть корректная конструкция PHP, так как она проходит через `eval()`.
# _BX_ERR_REP='E_ALL & ~E_NOTICE & ~E_STRICT & ~E_DEPRECATED'
